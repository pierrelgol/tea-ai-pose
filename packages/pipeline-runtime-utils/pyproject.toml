[build-system]
requires = ["hatchling>=1.24.2"]
build-backend = "hatchling.build"

[project]
name = "pipeline-runtime-utils"
version = "0.1.0"
description = "Shared runtime utilities for tea-ai pipeline"
requires-python = ">=3.12"
dependencies = [
  "numpy>=2.1.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/pipeline_runtime_utils"]
