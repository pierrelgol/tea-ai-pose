[build-system]
requires = ["hatchling>=1.24.2"]
build-backend = "hatchling.build"

[project]
name = "pipeline-config"
version = "0.1.0"
description = "Strict shared config and artifact layout for tea-ai pipeline"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/pipeline_config"]
